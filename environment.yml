name: ctrip
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain_community
      - langchain_core
      - langgraph
      - wikipedia
      - google-api-python-client
      - python-dotenv
      - tqdm
