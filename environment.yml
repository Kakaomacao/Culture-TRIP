name: ctrip
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain_community
      - langchain_core
      - langgraph
      - python-dotenv
      - tqdm
